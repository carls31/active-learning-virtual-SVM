#P:
#P:
#P:
#P:
#P:
#P:
row 36  trainControl -> how does actually work with the indexTrain
row 78  rbf_kernel -> that I added is required, right?
row 179 abs(pred) -> for the distance we need only positive values, right?
row 311 ref_added_or[1:250,]$distance = 1 -> which are the actual values for the distance?
row 655 binaryClassProblem -> A LIST WITH THE CLASSES NAME ISN'T ENOUGH?
